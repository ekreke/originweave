# obscura_kitesurf sample

Placeholder for the end-to-end sample migrated in **M0d** (spike 迁入 + fixtures).

Once populated this directory holds document A, its source snapshots, and the
recorded capability responses used by `make demo` / `make run`.
